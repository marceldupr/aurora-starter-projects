# Init — first-run provisioning

Schema, reports, and workflows are provisioned on first run or via `pnpm schema:provision`.

## Env vars

- `AURORA_API_URL` or `NEXT_PUBLIC_AURORA_API_URL`
- `AURORA_API_KEY`
